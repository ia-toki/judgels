apply from: '../../gradle/java.gradle'

dependencies {
    api project(':judgels-commons:judgels-persistence-core')
    api project(':judgels-commons:judgels-service-core')

    api "com.google.dagger:dagger:$daggerVersion"
    api "io.dropwizard:dropwizard-hibernate:$dropwizardVersion"

    compileOnly "com.google.dagger:dagger-compiler:$daggerVersion"

    annotationProcessor "com.google.dagger:dagger-compiler:$daggerVersion"
}
