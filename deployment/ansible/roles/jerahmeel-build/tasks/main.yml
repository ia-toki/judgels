- block:
    - name: Build jerahmeel image and push it to Docker Package
      docker_image:
        path: "{{ playbook_dir }}/../../../judgels-backends/jerahmeel/jerahmeel-dist"
        name: docker.pkg.github.com/ia-toki/judgels/jerahmeel
        force: yes
        push: yes
        buildargs:
          VCS_REF: "{{ lookup('env', 'TRAVIS_COMMIT') }}"
