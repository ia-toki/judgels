- block:
  - name: Build sealtiel image and push it to Docker Package
    docker_image:
      path: "{{ playbook_dir }}/../../../judgels-backends/sealtiel/sealtiel-dist"
      name: docker.pkg.github.com/ia-toki/judgels/sealtiel
      force: yes
      push: yes
      buildargs:
        VCS_REF: "{{ lookup('env', 'TRAVIS_COMMIT') }}"
