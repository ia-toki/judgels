- block:
    - name: Build jophiel image and push it to Docker Package
      docker_image:
        path: "{{ playbook_dir }}/../../../judgels-backends/jophiel/jophiel-dist"
        name: docker.pkg.github.com/ia-toki/judgels/jophiel
        force: yes
        push: yes
        buildargs:
          VCS_REF: "{{ lookup('env', 'TRAVIS_COMMIT') }}"
