export enum AnswerState {
  NotAnswered = 0,
  Answering,
  SavingAnswer,
  AnswerSaved,
  ClearingAnswer,
}

export enum StatementButtonText {
  Answer = 'Answer',
  Change = 'Change',
  Submit = 'Submit',
  Cancel = 'Cancel',
  ClearAnswer = 'Clear',
}
